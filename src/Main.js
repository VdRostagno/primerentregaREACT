import Container from "./Container"

const Main = ({ nombre }) => {

  return (
    <main>
      <h1>Hola, bienvenida {nombre}</h1>
      <span className="material-icons">
      </span>
      <Container />
    </main>
  )
}

export default Main
